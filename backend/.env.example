# ── Database ──────────────────────────────────────────────────────────────
DATABASE_URL="postgresql://leafpal:leafpal_dev@localhost:5432/leafpal"
# Cloud Run: DATABASE_URL="postgresql://USER:PASS@/leafpal?host=/cloudsql/PROJECT:REGION:INSTANCE"

# ── Auth ──────────────────────────────────────────────────────────────────
JWT_SECRET="change-me-in-production-use-long-random-string"

# ── Server ────────────────────────────────────────────────────────────────
PORT=3000
NODE_ENV=development
SKIP_AUTH=true

# ── Plant Identification API ──────────────────────────────────────────────
# https://my.plantnet.org/ — bos birakinca mock data kullanilir
PLANT_API_KEY=""

# ── Cloudinary (fotograf depolama) ────────────────────────────────────────
# https://cloudinary.com → Dashboard → API Keys
# Bos birakinca fotograflar local diske kaydedilir (dev only)
CLOUDINARY_CLOUD_NAME="Root"
CLOUDINARY_API_KEY="558387361781978"
CLOUDINARY_API_SECRET="9bCQWilPd-elReVTPX4lzvo2khg"

# ── Gemini AI (AI bitki tanisi) ───────────────────────────────────────────
# https://aistudio.google.com/app/apikey
GEMINI_API_KEY=""
